# Mutilate

A lightweight voice-chat-ready web scaffold that supports:

- Multi-model AI backend selection (Anthropic Claude, Google Gemini, OpenAI)
- MCP-style tool connection controls for GitHub and Jira
- Natural conversation orchestration state via a small client-side state manager

## Run locally

```bash
npm test
npm start
```

Then open <http://localhost:4173>.
